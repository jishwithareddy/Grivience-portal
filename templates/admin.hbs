<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Page</title>


<style>
.card-container {
  display: flex;
  flex-wrap: wrap;
}

.card {
  flex-basis: 30%;
  width: 300px;
  margin: 10px;
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 5px;
  box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
}

.card p {
  margin: 5px 0;
}

.card .actions {
  margin-top: 10px;
  display: flex;
  justify-content: space-between;
}

.card .actions a {
  text-decoration: none;
  color: #000;
  padding: 5px 10px;
  border: 1px solid #ccc;
  border-radius: 3px;
}

.card .actions a:hover {
  background-color: #ddd;
}
</style>


</head>
<body>
    
    <h1>Admin Page</h1>
    <div class="card-container">
      {{#each users}}
      <div class="card">
        <img src="{{this.profile_image}}" alt="User Profile Image">
        <div class="container">
          <h4><b>{{this.name}}</b></h4> 
          <p>Email: {{this.email}}</p> 
          <p>Phone: {{this.phone}}</p> 
          <p>Address: {{this.address}}</p> 
          <div class="actions">
            <a href="/admin/user/{{this._id}}/edit">Edit</a>
            <a href="/admin/{{this._id}}/delete" onclick="return confirm('Are you sure you want to delete this user?')">Delete</a>
          </div>
        </div>
      </div>
      {{/each}}
    </div>

</body>
</html>
